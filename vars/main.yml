---
# vars file for ferhaty.jenkins-slave

swarm_site: http://maven.jenkins-ci.org
swarm_dir: '/content/repositories/releases/org/jenkins-ci/plugins/swarm-client/'
swarm_base: "{{ swarm_site }}{{ swarm_dir }}"
